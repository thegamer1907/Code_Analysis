#include <bits/stdc++.h>#pragma GCC optimize("O3")#define FOR(j,k,n) for (int j = k; j < (int)(n); j++)#define MAXN 10000000000000000#define realMAXN 1000000000000000000#define fi first#define se second#define pb push_back#define test(t)  int t; cin>>t; for(int tes = 0; tes < t; tes++)#define fio ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0)using namespace std;typedef long long int lli;typedef pair<lli,lli> plli;const int MM = 1200000;lli m,n,k,p1,p2,p3,p,f,a[MM],vv[MM];string s,s1,s2;int main(){	// Have you solved the problem completely?      	    cin >> n;    for(int i=0; i<n; i++)    	cin >> a[i];    sort(a, a+n);    int cnt = 0;    p2 = n/2;    for(p1 = 0; p1 < n/2; p1++)    {    	while(p2 < n && a[p2] < 2*a[p1])    		p2++;    	if(a[p2] >= 2*a[p1])    		cnt++, p2++;    }    cout << n - cnt;	return 0;}