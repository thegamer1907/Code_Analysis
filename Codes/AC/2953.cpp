#include <bits/stdc++.h>#define fio ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0)using namespace std;typedef int lli;const int MM = 1200000;/* Print pair */template <typename T,typename S>ostream & operator << (ostream &os , const pair<T,S> &v) {    os << "(" ;    os << v.first << "," << v.second << ")" ;    return os ;}/* Print vector */template <typename T>ostream & operator << (ostream &os , const vector<T> &v) {    os << "[" ;    int sz = v.size() ;    for(int i = 0 ; i < sz ; ++i) {        os << v[i] ;        if(i!=sz-1)os << "," ;    }    os << "]\n" ;    return os ;}/* Print set */template <typename T>ostream & operator << (ostream &os , const set<T> &v) {    T last = *v.rbegin() ;    os << "[" ;    for(auto it : v) {        os << it  ;        if(it != last) os << "," ;    }    os << "]\n" ;    return os ;}/* Print Map */lli m,n,k,p1,p2,p3,p,f,a[MM];string s,s1,s2;vector<lli> zFunction(string &s) {    int n = (int) s.size();    vector<lli> z (n);    for (lli i=1, l=0, r=0; i<n; ++i) {        if (i <= r)            z[i] = min (r-i+1, z[i-l]);        while (i+z[i] < n && s[z[i]] == s[i+z[i]])            ++z[i];        if (i+z[i]-1 > r)            l = i,  r = i+z[i]-1;    }    return z;}bool isx[MM], isy[MM];int main(){fio; // remove during scanf              cin >> s;        n = s.length();        vector <int> val = zFunction(s);        s1 = s;    s1.pop_back();    vector <int> mid = zFunction(s1);    lli m1 = 0;    for(int i=0; i<mid.size(); i++) {        m1 = max(mid[i], m1);    }    lli m2 = 0;    for(int i=0; i<val.size(); i++) {        if(val[i] + i == n){            isy[val[i]] = 1;        }    }    for(int i=n-1; i>0; i--) {        if(isy[i] && i <= m1) {            cout << s.substr(0, i);            return 0;        }    }        cout << "Just a legend" ;        return 0;}